// Page temporarily disabled - restore full implementation when products are configured
// Full page content is preserved in git history (commit 424777b)
import { redirect } from 'next/navigation';

export default async function ToolsPage() {
  redirect('/');
}
