// Root layout required by Next.js App Router.
// Locale-specific html/body setup happens in [locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
